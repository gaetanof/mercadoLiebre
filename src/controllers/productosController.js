const controlador = {
    index: (req,res) => {
        res.send("index de productos")
    }, //mostrar listado de prod.
    show: //mostrar detalle de prod.
    create: // crear producto
};

module.exports = controlador;
